apiVersion: apps/v1
kind: Deployment
metadata:
  name: big-agi
spec:
  selector:
    matchLabels:
      app: big-agi
  template:
    metadata:
      name: big-agi
      labels:
        app: big-agi
    spec:
      hostUsers: false
      containers:
        - name: big-agi
          image: git.strypsteen.com/infra/big-agi
          env:
            - name: HTTP_BASIC_AUTH_USERNAME
              value: mathieu
            - name: OLLAMA_API_HOST
              value: https://textgen.strypsteen.com
            - name: PUPPETEER_WSS_ENDPOINT
              value: ws://browserless:3000
            - name: HTTP_BASIC_AUTH_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: big-agi
                  key: password
            - name: OPENROUTER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: big-agi
                  key: openrouter
      imagePullSecrets:
        - name: git-strypsteen-com
---
apiVersion: v1
kind: Service
metadata:
  name: big-agi
spec:
  selector:
    app: big-agi
  ports:
    - port: 3000
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: big-agi
spec:
  parentRefs:
    - name: envoy-internal
  hostnames:
    - llm.strypsteen.com
  rules:
    - backendRefs:
        - name: big-agi
          port: 3000
