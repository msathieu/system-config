exclude_patterns:
  - /run/host/lib/containers/storage/volumes/systemd-forgejo/_data/data/packages
  - /run/host/lib/containers/storage/volumes/systemd-forgejo/_data/data/repo-archive
postgresql_databases:
  - name: all
    hostname: systemd-postgresql
    username: postgres
    password: ${POSTGRES_PASSWORD}
