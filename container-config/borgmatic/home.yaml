exclude_patterns:
  - /run/host/lib/containers/storage/volumes/systemd-monero
postgresql_databases:
  - name: all
    hostname: systemd-postgresql
    username: postgres
    password: ${POSTGRES_PASSWORD}
  - name: all
    hostname: systemd-immich-postgresql
    username: postgres
    password: ${POSTGRES_PASSWORD}
