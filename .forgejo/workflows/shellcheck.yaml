name: ShellCheck
on: [push]
jobs:
  shellcheck:
    runs-on: debian-13
    steps:
      - name: Install ShellCheck
        run: apt-get update && apt-get install -y shellcheck
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run ShellCheck
        run: shellcheck *.sh
